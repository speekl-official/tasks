# Refuse to install a dependency version published less than 7 days ago.
# Guards against supply-chain attacks that publish a malicious version and
# get it picked up before it's pulled/flagged. Requires pnpm >=10.16.
minimum-release-age=10080
